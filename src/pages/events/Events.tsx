import Heading from "../../components/page-heading/Heading";
import "./Events.scss";

const Events = () => {
  return (
    <section className="events">
      <Heading />
    </section>
  );
};

export default Events;
