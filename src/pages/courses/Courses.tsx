import Heading from "../../components/page-heading/Heading";
import "./Courses.scss";

const Courses = () => {
  return (
    <section className="courses">
      <Heading />
    </section>
  );
};

export default Courses;
